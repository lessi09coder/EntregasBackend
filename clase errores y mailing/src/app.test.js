const {expect, assert} = require('chai');

describe('Test Login', () => {
    it('testeo de Usuario', () => {
        let numero  = 100;
        assert.equal(numero, 100)
    }) 

    it('testeo de Password', () => {
        let numero  = 80;
        assert.equal(numero, 100)
    }) 

})